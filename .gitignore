# ever seen a grown engineer go pale? they just found their .pp(k) in a git log
**/.ppk/*
**/.ssh/*
# ssh and tell no one

# build, coverage and dependencies, etc
node_modules/

# clean-adblock: ignore all config files except the examples
**/config.json
!**/config.json.example

# database, envpocalypse, init. configuration, logs and desktop services store
*.db
.env
*.ini
*.log
.DS_Store

# editor settings (don't tell your boss you use vscode)
.vscode/

# clean-adblock: user-generated files
example-list.txt
ca-dead-domains.txt
ca-redirect-domains.txt
ca-inconclusive-domains.txt
